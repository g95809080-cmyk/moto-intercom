# v1.2.0 正式版发布验证

发布构建源码：`408bfbe6ee871b6c5d2aeba55aa641ae9f9ad0bb`。该提交合并 origin/main，合并未产生文件变化。后续文档提交不改变 APK 源码。

## 构建与检查

在独立工作树的 O: 映射下使用 JDK F:/Android/jbr：

```powershell
./gradlew.bat testDebugUnitTest lintRelease assembleRelease --offline --console=plain
```

结果 `BUILD SUCCESSFUL in 2m 44s`。当前工程没有 testReleaseUnitTest 任务，实际运行的是既有 Debug 单测，Release Lint 和 Release 构建，不混淆测试 variant。

- 70 suites、566 tests，0 failures / errors / skipped。
- lintRelease：0 Error/Fatal、70 Warning。
- assembleRelease：通过，随后使用本地历史分发证书签名；未将私钥加入源码仓库。
- apksigner verify：v1、v2、v3 通过。
- aapt2 metadata：com.kuma.motointercom，versionName 1.2.0，versionCode 3，minSdk 23，targetSdk 36；无 application-debuggable。
- ABI：arm64-v8a、armeabi-v7a、x86、x86_64。

完整构建日志和元数据见 `evidence/2026-09-12-release/`。

## 覆盖安装

API 36 模拟器 emulator-5554 原有 1.1.0/code2 安装。对实际签名 Release APK 执行 adb install -r 成功，am start -W 返回 Status: ok。安装后 dumpsys package 显示 1.2.0/code3，flags 无 DEBUGGABLE。拒绝过的权限与未完成 TOUR 进度保留，启动后继续显示系统权限设置教学，截图为 release-upgrade.png。

## 产物身份

- 文件：MotoCom-v1.2.0.apk。
- SHA-256：`34E2ACCFDA3497D168959C0F3C88C476CAC46A514913C3D51E262D1265B37F3E`。
- 签名证书 SHA-256：`7F20F38DC1D7372CDE34CAC6E0E17D80EC995AC298C298FD0D24605E1A8070F3`。
- 证书与 v1.0.0 / v1.1.0 GitHub 资产一致。它是历史 Android debug 证书，用于个人分发升级兼容；本次 APK 为非 debuggable 的 Release 构建，不声称应用商店生产签名。

## 架构审查

独立 motointercom-product-architect reviewer：APPROVED；P0=0，P1=0。Base 为 0a67901，Head 为 408bfbe。核对版本、README、签名、非调试标记及本地门禁后，允许补齐文档/校验文件并按用户授权发布该 APK。剩余 Lint 提示非阻断。

本记录不代表双真机听音、蓝牙兼容性、射频或 CI 通过。本次只发行 Android APK；未改变原 release-v1 工作区的未提交内容。
