# KUM-49 纯域模型验证与源码审查

日期：2026-09-16。此记录是前轮 local-group-preflight 的后续：Linear 已恢复，并已建立真实 KUM-49；旧草案及前轮阻塞记录是历史快照，不再代表当前实施阻塞。

## 范围与提交

- Linear：https://linear.app/kuma999/issue/KUM-49
- Change：`kum-49-local-group-domain`；分支：`feat/kum-49-local-group-domain`。
- 文档/基线提交：`8c83dceda257263133ca995a0e693374e41018fb`。
- 源码提交：`bc545bdde61e6c3fc7de0653eb2d2bc5e6c1aead`。
- 源码仅新增 `group/GroupRoom.kt`、`GroupParticipation.kt` 和两个单测文件。没有修改旧双人路径、Service、UI、音频、协议、Gradle 或数据库。
- 功能包括随机六位码、独立房间身份、串行准入容量、60秒占位、满员等待、稳定ID移除限制、逐成员/控制连接代次、逐对就绪以及本机参与意图撤销。模型当前只由单测调用，APK没有多人入口。

## 自动化

JDK `F:/Android/jbr`，SDK `C:/Users/kuma/AppData/Local/Android/Sdk`，临时 M: 映射仅指向本 worktree。

1. `testDebugUnitTest --tests 'com.kuma.motointercom.group.*' --offline --console=plain`：新增22 tests通过，0失败/错误/跳过。
2. `testDebugUnitTest lintDebug assembleDebug --offline --console=plain`：BUILD SUCCESSFUL in 1m 39s；72 suites、588 tests，0失败/错误/跳过；Lint 0 Error/Fatal、69 Warning，与基线警告数量相同；Debug APK构建成功。
3. `rasen validate kum-49-local-group-domain --strict --json`：通过。Rasen命令均设置 DO_NOT_TRACK=1、RASEN_TELEMETRY=0。
4. 源码提交前后明确路径暂存。复审指出design.md EOF多空行，随后仅规范化文档结尾；无源码变化，最终差异检查通过。

日志及新增测试XML见 `evidence/2026-09-16-kum-49/`。不因后续只修改文档而重复执行已通过的源码测试。

## 独立 motointercom-product-architect 源码复审

APPROVED：仅 KUM-49 纯域模型源码。

P0：无。早期发现缓存驱逐可能让已撤销JOIN重放，已改为未过期缓存满时BUSY拒绝，证明有效期最多10秒，回执保留30秒；覆盖Leave及Remove→Unblock后的缓存压力重放。

P1：无。早期发现旧链路失败可能重建新链路，已要求expected旧link lease及两端当前成员lease，并测试新链路就绪/成员替换之后的迟到重建。

Non-blocking：本机token含runtime UUID，进程重新创建不与旧generation碰撞；完整域模型无生产入口，真实音频/网络验证不在此批准范围。reviewer独立读取新增22 tests XML，没有重复跑构建。其指出的唯一文档EOF空行已清理。

Base SHA：`8c83dceda257263133ca995a0e693374e41018fb`

Head SHA：`bc545bdde61e6c3fc7de0653eb2d2bc5e6c1aead`

Next gate allowed：补齐上述验证后进入本issue的Draft PR和Linear审查回填，不自动合并。协议、认证发现、Wi-Fi Direct、WebRTC、共享音频与UI仍需后续独立设计门禁。

## 尚未完成

- GitHub CI状态以PR实际检查为准，不将本地通过写成远端CI通过。
- D01–D12仍未执行；当前无真实设备。三机/四机听音、无路由器凭码建网、来电/蓝牙、两小时稳定性与四小时观察均未验收。
- KUM-49进入审查不代表四人产品完成；用户PRD仍完整生效。
- 本次运行中出现 `.codex/` 和 `rasen/config.yaml` 的Rasen 0.1.5工具升级差异。未纳入功能提交，未回滚这些来源未完全确认的改动；仅清理本次测试临时日志和盘符映射。
